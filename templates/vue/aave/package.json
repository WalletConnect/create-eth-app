{
  "name": "@aave-app/monorepo",
  "version": "1.0.0",
  "keywords": [
    "aave",
    "ethereum",
    "vue",
    "workspaces",
    "yarn"
  ],
  "private": true,
  "scripts": {
    "vue-app:build": "yarn workspace @aave-app/vue-app build",
    "vue-app:lint": "yarn workspace @aave-app/vue-app lint",
    "vue-app:serve": "yarn workspace @aave-app/vue-app serve"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
